#
# Copyright (C) 2022 Nethesis S.r.l.
# SPDX-License-Identifier: GPL-3.0-or-later
#

#
# This systemd unit starts a umami instance using Podman.
# Most parts of this file come from podman-generate-systemd.
#

[Unit]
Description=Podman umami.service
Requires=postgresql-app.service umami-app.service
Before=postgresql-app.service umami-app.service

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
EnvironmentFile=-%S/state/environment
Restart=always
TimeoutStopSec=70
ExecStartPre=/bin/rm -f %t/umami.pid %t/umami.pod-id
ExecStartPre=/usr/bin/podman pod create --infra-conmon-pidfile %t/umami.pid \
    --pod-id-file %t/umami.pod-id \
    --name umami \
    --publish 127.0.0.1:${TCP_PORT}:80 \
    --replace
ExecStart=/usr/bin/podman pod start --pod-id-file %t/umami.pod-id
ExecStop=/usr/bin/podman pod stop --ignore --pod-id-file %t/umami.pod-id -t 10
ExecStopPost=/usr/bin/podman pod rm --ignore -f --pod-id-file %t/umami.pod-id
PIDFile=%t/umami.pid
Type=forking

[Install]
WantedBy=default.target
